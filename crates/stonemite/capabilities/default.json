{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Minimal core permissions for the bundled Stonemite settings window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
